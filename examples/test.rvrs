flow bless(user: Identity) {
  source trust = check_trust(user)
  delta trust = false
  mouth "Trust evaluated"
  echo "blessed"
}
flow bless(user: Identity) {
  source trust = check_trust(user)
  delta trust = false
  mouth "Trust evaluated"
  echo "blessed"
}
