flow full_test() {
  pillar ROLE = "admin"
  delta x = 5
  delta check = x == 5
  mouth "start"
  branch (check) {
    echo "matched"
    delta x = 10
  } else {
    echo "did not match"
  }
  echo "done"
}
